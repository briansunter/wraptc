{
  "routing": {
    "defaultOrder": ["system-provider"]
  },
  "providers": {
    "system-provider": {
      "binary": "/usr/bin/system-provider",
      "args": [],
      "jsonMode": "none",
      "streamingMode": "line",
      "capabilities": ["generate"]
    }
  }
}
