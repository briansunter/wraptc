{
  "name": "@wrap-terminalcoder/mcp-server",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "wtc-mcp": "./dist/server.js"
  },
  "scripts": {
    "build": "bun build ./src/server.ts --outdir ./dist --target bun",
    "dev": "bun --watch src/server.ts"
  },
  "dependencies": {
    "@wrap-terminalcoder/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
