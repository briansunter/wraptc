# npm registry configuration
# This file is used by CI/CD and should NOT contain actual tokens
# Tokens are injected via environment variables (NPM_TOKEN)

# Ensure we always use the official npm registry
registry=https://registry.npmjs.org/

# Don't create package-lock.json (we use bun.lockb)
package-lock=false

# Allow publishing same version multiple times (useful for recovery)
force=false

# Strict peer dependency checking (can be disabled if needed)
strict-peer-dependencies=false

# Access level (public by default for scoped packages)
access=public

# Always authenticate when publishing
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Provenance transparency (requires npm 9.8.0+)
# Uncomment when ready to enable provenance:
# provenance=true
