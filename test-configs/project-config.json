{
  "routing": {
    "defaultOrder": [
      "project-provider",
      "user-provider",
      "system-provider"
    ]
  },
  "providers": {
    "project-provider": {
      "binary": "./project-provider",
      "args": [
        "--project-flag"
      ],
      "jsonMode": "none",
      "streamingMode": "line",
      "capabilities": [
        "generate",
        "edit",
        "test"
      ]
    }
  }
}
